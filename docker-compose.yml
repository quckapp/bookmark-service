version: '3.8'

services:
  bookmark-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quckapp-bookmark-service
    ports:
      - "5010:5010"
    environment:
      - PORT=5010
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=password
      - DB_NAME=quckapp_bookmarks
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - quckapp-network
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    container_name: quckapp-bookmark-mysql
    ports:
      - "3311:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=quckapp_bookmarks
    volumes:
      - bookmark-mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - quckapp-network

  redis:
    image: redis:7-alpine
    container_name: quckapp-bookmark-redis
    ports:
      - "6384:6379"
    networks:
      - quckapp-network

volumes:
  bookmark-mysql-data:

networks:
  quckapp-network:
    driver: bridge
